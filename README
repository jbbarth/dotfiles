FIRST INSTALL GIT:

apt-get install git-core

DEPLOY ON A NEW MACHINE/ACCOUNT (with zsh shell):

cd
git init
git pull git://github.com/jbbarth/127.0.0.1.git
sudo ./scripts/debian/first-packets.sh
source .zshrc
